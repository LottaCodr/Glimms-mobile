# Keep source and configuration files consistent across Windows, macOS, and Linux.
* text=auto eol=lf

# Preserve Windows-native command files if they are added later.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
